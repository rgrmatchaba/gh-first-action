name: First Workflow

on: workflow_dispatch

jobs: 
  first-job: 
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
          echo "Hello World"
          echo "I am here to dominate"
      - name: Print Goodbye
        run: echo "Bye"
